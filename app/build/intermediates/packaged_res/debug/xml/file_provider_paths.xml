<?xml version="1.0" encoding="utf-8"?>
<!--
    FileProvider path configuration.
    Exposes files inside context.filesDir so the generated chime WAV
    can be shared as a URI with the notification system.
-->
<paths>
    <files-path name="sounds" path="." />
    <cache-path name="scan_reports" path="scan_reports/" />
</paths>
